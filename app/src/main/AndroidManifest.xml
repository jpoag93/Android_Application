<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstactivityset">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Decision Decider"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstActivitySet">
        <activity android:name=".MainActivity4" android:label="Preferences"></activity>
        <activity android:name=".MainActivity3" android:label="Help"/>
        <activity android:name=".MainActivity2" android:label="Yay!"/>
        <activity android:name=".MainActivity" android:label="Decision Decider">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>